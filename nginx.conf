# For more information on configuration, see:
#   * Official English Documentation: http://nginx.org/en/docs/
#   * Official Russian Documentation: http://nginx.org/ru/docs/

user root;
worker_processes auto;
error_log /var/log/nginx/error.log;
pid /run/nginx.pid;

# Load dynamic modules. See /usr/share/doc/nginx/README.dynamic.
include /usr/share/nginx/modules/*.conf;

events {
    worker_connections 1024;
}

http {
    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    sendfile            on;
    tcp_nopush          on;
    tcp_nodelay         on;
    keepalive_timeout   65;
    types_hash_max_size 4096;

    include             /etc/nginx/mime.types;
    default_type        application/octet-stream;


    server {
        listen       80;
        # listen       [::]:80;
        server_name  _;
        root         /dist;

        add_header Cross-Origin-Opener-Policy "same-origin";
        add_header Cross-Origin-Embedder-Policy "require-corp";
        # 使用 ^~ 提高此前缀匹配的优先级
        location ^~ /api/ {
            proxy_pass http://81.70.39.188:8201;
        }

        location ^~ /assets/ {
            alias /dist/assets/;
        }

        location ~ /* {
            try_files $uri $uri/ /index.html;
            index index.html;
        }

        location /healthcheck {
            default_type text/plain;
            return 200 "ok";
        }
        index index.html index.htm;
        access_log /dist/access.log;
        error_log /dist/error.log;
    }

    server {
        listen       443 ssl;
        server_name  school1.agrox.horai.cn;
        root         /dist;

        ssl_certificate  /etc/nginx/cert/data.agrox.horai.cn/cert.pem;
        ssl_certificate_key /etc/nginx/cert/data.agrox.horai.cn/cert.key;

        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_prefer_server_ciphers on;
        ssl_ciphers 'ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-AES128-SHA:ECDHE-RSA-AES128-SHA:ECDHE-ECDSA-AES256-SHA:ECDHE-RSA-AES256-SHA:DHE-RSA-AES128-SHA:DHE-RSA-AES256-SHA:ECDHE-ECDSA-DES-CBC3-SHA:ECDHE-RSA-DES-CBC3-SHA:EDH-RSA-DES-CBC3-SHA:AES128-GCM-SHA256:AES256-GCM-SHA384:AES128-SHA:AES256-SHA:DES-CBC3-SHA:!DSS';

        add_header Cross-Origin-Opener-Policy "same-origin";
        add_header Cross-Origin-Embedder-Policy "require-corp";

        # 使用 ^~ 提高此前缀匹配的优先级
        location ^~ /api/ {
            proxy_pass http://81.70.39.188:8201;
        }

        location ^~ /assets/ {
            alias /dist/assets/;
        }

        location ~ /* {
            try_files $uri $uri/ /index.html;
            index index.html;
        }

        location /healthcheck {
            default_type text/plain;
            return 200 "ok";
        }

        index index.html index.htm;
        access_log /dist/access.log;
        error_log /dist/error.log;
    }
}
